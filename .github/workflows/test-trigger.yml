name: Test Workflow Trigger

on:
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Test trigger
      run: |
        echo "✅ Workflow triggered successfully at $(date)"
        echo "This confirms GitHub Actions is working"